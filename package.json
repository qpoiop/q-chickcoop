{
  "name": "chickcoop",
  "version": "1.0.0",
  "description": "CHICKCOOP — AAA-style twin-stick action roguelite (Three.js)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "deploy": "vite build && wrangler deploy"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "wrangler": "^4.113.0"
  }
}
